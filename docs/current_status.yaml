project:
  name: regional-demand-forecasting-optimizer-v2
  repo_type: clean rebuild
  active_repo_path: /content/regional-demand-forecasting-optimizer-v2
  active_phase: "Phase 0 — Setup / continuity / core utilities"
  active_block: "Bootstrap system + prompt system"

last_session:
  date: "2026-04-17"
  summary: >
    Created fresh V2 repo scaffold from scratch, initialized git, committed initial
    structure, added reusable git workflow thinking, and started setting up prompt
    continuity + session organization.
  accomplishments:
    - "Built professional folder structure"
    - "Initialized git and completed first commit"
    - "Created continuity prompts"
    - "Started planning startup/bootstrap workflow"
  blockers:
    - "Repo/data continuity still needs automation"
    - "Processed dataset not yet wired cleanly into V2"
    - "Core src modules still placeholders"
  next_goal: >
    Build continuity files and startup checks, then populate the first core modules
    and connect data cleanly.

current_state:
  current_notebook: "Colab setup / continuity session"
  current_script: "src/utils/session_bootstrap.py"
  current_data_location: "To be linked from old repo or Google Drive"
  current_priority: "Continuity system + project bootstrap"
  baseline_best_model: "XGBoost"
  baseline_best_wmape: 0.1106

validated_baselines:
  xgboost_wmape: 0.1106
  prophet_wmape: 0.1116
  ma7_wmape: 0.1925
  roll_14_importance: 0.4682

known_issues:
  - "West region performs worse than other regions"
  - "Beauty category has high variability"
  - "Current safety stock logic likely over-flags urgency"
  - "No uncertainty quantification yet"
  - "Need to migrate validated baseline work into V2 cleanly"

next_priorities:
  - "Create session bootstrap utility"
  - "Create core utility modules (metrics, loader, validation)"
  - "Connect processed feature dataset"
  - "Start LightGBM benchmark"
  - "Add CatBoost after LightGBM"

recent_files_touched:
  - "README.md"
  - "requirements.txt"
  - ".gitignore"
  - "docs/prompts/continuity_prompt_compact.md"
  - "docs/prompts/coding_session_prompt.md"
  - "docs/session_log.md"

session_defaults:
  environment: "Google Colab"
  working_repo: "regional-demand-forecasting-optimizer-v2"
  preferred_style: "practical, human, production-aware"
